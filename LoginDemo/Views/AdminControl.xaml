<UserControl x:Class="LoginDemo.Views.AdminControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LoginDemo.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Админ панель - пользователи"
                   FontSize="22"
                   Margin="0 0 0 15"
                   HorizontalAlignment="Center"/>
        <DataGrid x:Name="UsersGrid"
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  Margin="0 0 0 15"
                  Height="200"
                  CellEditEnding="UsersGrid_CellEditEnding">

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}"
                                    IsReadOnly="True"/>
                <DataGridTextColumn Header="Логин" Binding="{Binding Login}"/>
                <DataGridTextColumn Header="Пароль" Binding="{Binding Password}"/>
                <DataGridTextColumn Header="Роль" Binding="{Binding Role}"/>

                <DataGridTemplateColumn Header="Удалить">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="X"
                                    Width="30"
                                    Click="DeleteUser_Click"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Row="2" 
            Orientation="Horizontal"
            Margin="0,10,0,10">

            <TextBox x:Name="InputLogin" 
             FontSize="10"
             FontStyle="Italic" 
             Width="120" 
             Text="Введите логин"/>

            <TextBox x:Name="InputPassword" 
             FontSize="10"
             FontStyle="Italic" 
             Width="120" 
             Text="Введите пароль"/>

            <ComboBox x:Name="InputRole" 
              Width="120">
                <ComboBoxItem Content="User"/>
                <ComboBoxItem Content="Admin"/>
            </ComboBox>

            <Button Content="Добавить" 
            Width="100"
            Click="AddUser_Click"/>
        </StackPanel>
        <Button Grid.Row="3" Content="Назад"
                Width="120" HorizontalAlignment="Center"
                Click="Button_Click"/>
    </Grid>
</UserControl>
